type Artist @entity(immutable: true) {
  id: Bytes!
  account: Bytes!
  title: String!
  platformId: BigInt!
}

type _Schema_
  @fulltext(
    name: "artistSearch"
    language: en
    algorithm: rank
    include: [{ entity: "Artist", fields: [{ name: "title" }] }]
  )
