{
  "name": "cloud_functions",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "emulate-firestore": "gcloud emulators firestore start --host-port=localhost:8085",
    "emulate-pinFile": "npx functions-framework --target=pinFile --source=index.js --port=8081 --env-vars=\"INFURA_API_KEY=$INFURA_API_KEY,INFURA_API_SECRET=$INFURA_API_SECRET,ALLOWED_ORIGINS=*\"",
    "emulate-trackPlayback": "FIRESTORE_EMULATOR_HOST=localhost:8085 npx functions-framework --target=trackPlayback --source=index.js --port=8080 --env-vars=\"FIRESTORE_PROJECT_ID=justicantus-development,ALLOWED_ORIGINS=*\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/firestore": "6.6.1",
    "@google-cloud/functions-framework": "^3.2.0",
    "axios": "^1.4.0",
    "busboy": "^1.6.0",
    "ethers": "5.7.0",
    "form-data": "^4.0.0"
  }
}
